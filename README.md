# lightC

LightC is a simple, intuitive language, strongly inspired by C, using concepts such as garbage collection, object orientation, modularity, access control, exceptions ...

The LightC compiler (lcc) is currently under development, targeting the LuaJIT virtual machine.
